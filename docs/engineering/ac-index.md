# AC index — every acceptance criterion mapped to its proof

Generated reference, per `docs/engineering/repo-map.md`: each AC from
`docs/engineering/implementation-plan.md` maps to the CI job (or manual procedure) that proves
it, and the file where that proof lives. CI fails if an AC in the plan has no row here
(addendum §25). Add a row in the same PR that lands the AC's proof.

| AC | CI job | Proof | Notes |
|---|---|---|---|
| M2-AC1 (slice, end to end) | `test:e2e` | `tests/e2e/vertical-slice-e2e.spec.ts` | Step 9 red pending [#31](https://github.com/drewdrewthis/playa-post/issues/31) |
| M2-AC15 (composition assertion, B12) | `test:security` | `composition-assertion.security.test.ts` | |
| M2-AC16 (log hygiene) | `test:integration` | `tests/integration/vertical-slice-log-hygiene.integration.test.ts` | |
| M2-AC20 (viewerId provenance, B14) | `test:security` | `viewer-id-provenance.security.test.ts` | |
| M2-AC26 (regression) | `lint:boundaries` | `b-rows.manifest.json` | Also covered by `test:security`'s manifest check |
| M5-B1a (multi-hop reach + who-can-see-you radius) | `test:integration` | `modules/identity/tests/integration/visible-to-distance.integration.test.ts` | Degree ≥2 reach; absence-not-anonymity beyond a person's own limit; service round-trip. Ghost surrogate IDs remain open ([ADR-0004] decision 4 deviation, recorded in migration 20260809140000) |
| Bulletin post types ([#87](https://github.com/drewdrewthis/playa-post/issues/87)) | `test:integration` | `modules/bulletins/tests/integration/bulletin-post-types.integration.test.ts` | Six postable types round-trip; `type:` filter narrows; `update`/`note` refused at the write surface. Feature: `specs/features/bulletin-post-types.feature` |
| Pin a note — delivery and degree-1 gate ([#88](https://github.com/drewdrewthis/playa-post/issues/88)) | `test:integration` | `modules/notes/tests/integration/pin-a-note.integration.test.ts` | Recipient-only delivery; §6a author disclosure read at query time (sever/deactivate/downgrade after the pin move the card, never the note); second-degree, stranger, and self refusals indistinguishable and writing zero rows; content bounds; `note.pin` sync replay with a body-free idempotency echo (`app.mutation_results` keeps no copy of the text); outbox payload carries identifiers only. Feature: `specs/features/pin-a-note.feature`; decision D6 |
| Pin a note — schema and authorized set ([#88](https://github.com/drewdrewthis/playa-post/issues/88)) | `test:integration` | `modules/notes/tests/integration/visible-notes-migration.integration.test.ts` | `app.notes` RLS backstop + `app_rw`-only grants; `app.visible_notes` is `SECURITY INVOKER` with `search_path = ''`; checked-in SQL appears verbatim in exactly one migration; no tsvector over a note |
| Pin a note — notes never re-derive reachability ([#88](https://github.com/drewdrewthis/playa-post/issues/88)) | `test:unit` | `tests/fitness/sql-table-ownership.fitness.test.ts` | `modules/notes`' checked-in SQL is held to `app.notes` + `app.visible_*`, and is not allowlisted for `app.connections`/`app.users` (ADR-0002 §6, R2). Composition itself asserted by `modules/notes/tests/unit/visible-notes-sql-composition.unit.test.ts` |
| Edit your display name ([#177](https://github.com/drewdrewthis/playa-post/issues/177)) | `test:integration` | `modules/identity/tests/integration/edit-display-name.integration.test.ts` | Driven through the module's own router, so "authorized server-side" is a claim about the transport: one identical payload renames two different callers, and a payload naming a person is refused rather than stripped. Handle untouched and still resolving; a submitted handle refused (ADR-0008 rule 4, D15). AC5 proved against the real §6a projection — the connected viewer's next read returns the new name with nothing published, and a viewer the projection withholds a name from still gets none. No migration. Feature: `specs/features/edit-display-name.feature`; decision D15 |
| Edit your display name — shared bounds and the rename use case ([#177](https://github.com/drewdrewthis/playa-post/issues/177)) | `test:unit` | `modules/identity/tests/transport/update-display-name.input.unit.test.ts`, `modules/identity/tests/application/update-display-name.service.unit.test.ts`, `apps/web/src/app/profile/display-name-editor.unit.test.tsx` | The edit schema is asserted *against onboarding's schema* rather than against copied literals, which is the only way the one-source-of-truth claim can fail loudly; the service answers with the stored name rather than its argument; the You screen's editor sends the name and nothing else, trims before sending, refuses to send an empty name, keeps the typed name on a refusal, freezes the field while the save is out, and takes focus on open and hands it back to the Edit button on close. Browser walk: `tests/e2e/you-screen.spec.ts` (CI-advisory) — the one `@e2e` scenario, renaming user A against the real server and asserting the heading comes back carrying the *refetched* name, then restoring the name in a `finally` because the e2e users are shared in sequence |
| Pin a note — compose sheet, board rows, and the degree-gated control ([#88](https://github.com/drewdrewthis/playa-post/issues/88)) | `test:unit` | `apps/web/src/app/notes/{pin-note-draft,note-recipient,note-reach,note-board-items,pin-note-outcome,pin-note-submit,note-author}.unit.test.ts`, `apps/web/src/app/offline/sync-runner.unit.test.ts` | Draft bounds mirrored ahead of the round trip; §6a withheld name renders "their board" in every sentence; the pin control offered only at degree 1 and the comp's hint otherwise; notes interleaved by time and absent from every search; refusals rendered without disclosing anything about the recipient. No `@e2e` — `tests/e2e/global-setup.ts` onboards two users who are not connected, and `apps/web`'s unit project has no component harness, so the decisions live in pure modules |
| Pin a note — a queued note replays exactly once ([#88](https://github.com/drewdrewthis/playa-post/issues/88)) | `test:unit` | `apps/web/src/app/offline/replay-routes.unit.test.ts` | `QUEUED_MUTATION_TYPES` and the drainer's routing table held together: every queued type has exactly one route, and `note.pin` takes `sync.submitMutations` — the only path that deduplicates it. Server half: the sync-replay scenario in `modules/notes/tests/integration/pin-a-note.integration.test.ts` |
| Pinned notes reach the bell ([#149](https://github.com/drewdrewthis/playa-post/issues/149)) | `test:integration` | `modules/notifications/tests/integration/notifications-list.integration.test.ts` | `NotePinned` gains its consumer (closes [#112](https://github.com/drewdrewthis/playa-post/issues/112)'s open event): a delivered note lists as a `kind: 'note'` notification carrying `noteId` only — no body, no author; an undelivered event lists nothing; the read re-checks `app.visible_notes` so a note that leaves the authorized set leaves the bell, and a payload that names the wrong recipient discloses nothing; dismissal marks it read; redelivery still lists one. On by default because no preference exists — M5 owns preferences |
| Pinned notes — the drainer is actually wired to the consumer ([#149](https://github.com/drewdrewthis/playa-post/issues/149)) | `test:integration` | `apps/server/src/composition/container-notification-wiring.integration.test.ts` | The real `buildAppContainer` drains a `NotePinned` row and its `DeliverNotePinnedHandler` receipt lands. The only proof of registration: every module suite wires its own module, so an unregistered consumer is a feature that silently does not exist |
| Pinned notes — the bell reads both kinds ([#149](https://github.com/drewdrewthis/playa-post/issues/149)) | `test:unit` | `apps/server/src/modules/notifications/tests/unit/list-notifications-unread.unit.test.ts`, `apps/web/src/app/notifications/notifications-view.unit.test.ts` | Notes are singletons — never windowed, however close together they were pinned — and share one newest-first list with grouped bulletins; `notificationTitle` branches on `kind` and names nobody ("Someone pinned a note to your board"), because the contract carries no author |
| A via passes an intro on with a note of their own ([#175](https://github.com/drewdrewthis/playa-post/issues/175)) | `test:integration` | `modules/intros/tests/integration/request-an-intro.integration.test.ts`, `modules/intros/tests/integration/intro-requests-migration.integration.test.ts` | The note is required on `pass_on` and refused on `decline`; stored on the same row by the same statement as the decision, trimmed; the target reads both notes with both authors' §6a cards, the via's projected from their own `app.visible_people(via, 0, 1)` self-projection so a severed connection cannot leave an unsigned vouch; neither note reaches `intros.listOutbox`, the decide receipt, an outbox payload, or a log line — asserted over **two** distinctive phrases, one per author. Catalog half: the nullable `via_note` column and its `intro_requests_via_note` CHECK, an implication rather than an equality so pre-#175 pass-ons stay valid. Feature: `specs/features/request-an-intro.feature`; decision D11 |
| A via passes an intro on with a note of their own — the two rules and the wire shape ([#175](https://github.com/drewdrewthis/playa-post/issues/175)) | `test:unit` | `modules/intros/tests/unit/{intro-note.policy,decide-intro.input,intro-request.events}.unit.test.ts`, `apps/web/src/app/intros/{intro-inbox,intro-copy}.unit.test.ts(x)` | `validateViaNote` is the one function that branches on the decision, so the service stays branch-free; `decideIntroInput` is a discriminated union whose decline arm is a `strictObject` — a note there is refused, never stripped; `introDecided`'s payload is byte-identical with and without a via note; the inbox's Pass on opens a required field and moves focus into it, submits the trimmed note as one command, and renders the target's two notes under two cards with no placeholder for a pre-#175 absence |
| A passed-on intro can be accepted, and accepting connects ([#166](https://github.com/drewdrewthis/playa-post/issues/166)) | `test:integration` | `modules/intros/tests/integration/request-an-intro.integration.test.ts`, `modules/intros/tests/integration/intro-requests-migration.integration.test.ts`, `apps/server/src/composition/container-notification-wiring.integration.test.ts` | **AC1/AC2**: the target's row is answerable and an acceptance records `accepted` + `responded_at`, emits `IntroAccepted` in the same transaction, and the **event** is what writes `app.connections` — asserted unconnected before delivery and connected after, at an accepted invite's own `accepted`/`full`/`full`, with `app.visible_people` then answering degree 1 both ways. **AC3**: a decline stores `target_declined` (distinguishable in history from `accepted` and from an unanswered `passed_on`) and connects nobody even when every event is delivered; the requester's own record reads a decline and an unanswered introduction identically. **AC4**: requester, via, fourth party and an id naming nothing are four refusals serialized into a one-element `Set`. **AC5**: a second answer in either direction is refused without moving `responded_at`, a race leaves one winner, and an open or via-declined request cannot be answered at all — refused *identically*, so a target cannot detect a via's decline by trying to accept. Redelivery and an already-connected pair each add no row and announce no second `ConnectionAccepted`. Catalog half: `responded_at` nullable + the `intro_requests_responded_at` equality CHECK, the five-value `intro_requests_status`, the widened `intro_requests_via_note` (its #175 form refuses the very UPDATE that accepts a vouched introduction), and no new index. Registration half — the only check that catches an unregistered consumer, whose absence is silent. Feature: `specs/features/request-an-intro.feature`; decision D12; ADR-0017 D9 |
| A passed-on intro can be accepted — the seam, the wire, and the row ([#166](https://github.com/drewdrewthis/playa-post/issues/166)) | `test:unit` | `modules/intros/tests/unit/{intro-response,respond-to-intro.input,intro-request.events}.unit.test.ts`, `modules/connections/tests/domain/introduced-pair.unit.test.ts`, `apps/web/src/app/intros/{intro-inbox,intro-copy}.unit.test.ts(x)` | **AC6** (the recorded decision): a token-free mutation, with the connection formed from a published event — `INTRO_RESPONSE` is its own vocabulary disjoint from `INTRO_DECISION`, `STATUS_FOR_RESPONSE` maps to statuses no via decision produces, and `INTRO_UNAVAILABLE` stays the one refusal on the path. `introResponded` reads its type from the stored status and its `occurredAt` from `responded_at` rather than the via's `decided_at`; `introDecided` now refuses an answered row instead of calling it a decline. The wire refuses a note, a self-named status and every spelling of an actor id. `toIntroducedPair` reads the pair from the payload or throws — never a guess, never a skip. The web row offers Accept and Decline under their own test ids, reaches `intros.respond` and never `intros.decide`, says a refusal reaches nobody before it is pressed, and confirms an acceptance as under way rather than done. Browser walk: `tests/e2e/intro-request.spec.ts` (CI-advisory) |
| Sign-in email carries a one-time code the installed PWA can verify itself ([#179](https://github.com/drewdrewthis/playa-post/issues/179)) | `test:unit` | `apps/web/src/app/auth/supabase-auth-client.unit.test.ts`, `apps/web/src/app/auth/sign-in-failure.unit.test.ts`, `tests/e2e/sign-in-code.spec.ts` | AC3 (code completes sign-in inside the installed PWA) and AC4/AC5 (rejected code, with the "send a new code" recovery path) proven end to end in `tests/e2e/sign-in-code.spec.ts`; AC4/AC5/AC6 collapse into one rejection branch — `describeSignInFailure`'s `isCodeRejected` case — per decision D10, because GoTrue cannot tell a wrong code from an expired one from a reused one. `test:e2e` is CI-advisory (`docs/engineering/l5-plan.md` D2), so `test:unit` is the gating proof |
| Dismiss becomes a Dismissed category, and is reversible ([#170](https://github.com/drewdrewthis/playa-post/issues/170)) | `test:integration` | `modules/moderation/tests/integration/dismissed-category.integration.test.ts` | **AC1/AC5** (viewer-local, alters nothing): dismissing leaves the author's own row unarchived at its original `version`, and the author's own Dismissed category empty. **AC2**: the bulletin stays off the dismisser's board throughout. **AC3**: `bulletins.dismissed` lists it with its §6a author card, newest **dismissal** first (not newest post), scoped to the caller, and carrying nothing the viewer *reported* — the assertion that keeps M2-AC10/B9's "no what-have-I-reported surface" true. A dismissed bulletin whose author has since removed it drops out of the list while its dismissal row survives, because the category composes `app.visible_bulletins` rather than joining `app.bulletins`. **AC4**: `moderation.undismiss` puts it back on the board and empties the category; un-dismissing something never dismissed converges instead of refusing; un-dismissing something also *reported* withdraws only the dismissal, and the bulletin stays hidden. Unrelated actor: `NOT_FOUND`, byte-identical to a UUID that never existed, with zero dismissal rows and zero outbox rows. **AC6** (the recorded decision): dismiss and un-dismiss are board-bulletin actions only — the intro inbox keeps #166's terminal decline and gains no dismiss affordance, and un-dismiss is online-only, symmetric with dismiss until [#63](https://github.com/drewdrewthis/playa-post/issues/63) gives both a replay route. Feature: `specs/features/moderation-report-dismiss.feature`; decision D13; ADR-0005 gains a `bulletin.undismiss` row |
| Dismissed category — the composition rule and the write-path gate ([#170](https://github.com/drewdrewthis/playa-post/issues/170)) | `test:unit` | `modules/bulletins/tests/unit/list-dismissed-bulletins.unit.test.ts`, `apps/web/src/app/bulletins/dismissed-view.unit.test.ts` | Server: the category is dismissal-order restored over an authorized read that promises no order, drops what the viewer may no longer see, and bounds the identifier read by `BOARD_PAGE_SIZE` rather than a second constant. Web: `?view=` parses to the board for anything unrecognised — a near-miss must never open somebody's private list; the list reports a failed read as a failure and never as "nothing dismissed"; an un-dismissal that never reached a verdict offers a retry and a judged refusal does not; the category marks the viewer's own posts as theirs from `listMine`'s identifiers rather than from the §6a author card, because a viewer may dismiss their own bulletin and a card rendered as a stranger's would offer them the controls for reaching themselves. Write-path half is `test:security` — see the next row. Browser walk: the Dismissed-category round trip in `tests/e2e/vertical-slice-e2e.spec.ts` (CI-advisory) |
| Dismissed category — un-dismissal cannot reach another viewer's row ([#170](https://github.com/drewdrewthis/playa-post/issues/170)) | `test:security` | `tests/security/write-path-idor-bulletins.security.test.ts` | B13 gains `bulletin.undismiss`. Two shapes, because a delete has two: an actor who cannot see the bulletin is refused with zero rows and zero outbox rows, and an actor who legitimately *can* see it still cannot clear somebody else's dismissal — the `viewer_id = <actor>` predicate in the delete is the only thing enforcing that, and nothing above the SQL would notice its removal. Also asserts an un-dismissal never deletes a report, whose loss would be silent (M2-AC10, B9) |
| A note opens into an expanded view, backed by a read procedure ([#176](https://github.com/drewdrewthis/playa-post/issues/176)) | `test:integration` | `modules/notes/tests/integration/read-a-note.integration.test.ts` | **AC1/AC3** (tapping a note opens an expanded view; a backing read procedure exists): `notes.getById` answers the recipient the same `Note` the list carries — asserted field for field against `notes.list`, because the expanded view shows more *of* the note and never more *about* it. **The refusal is the security half**: a stranger naming a real note, a stranger naming no note, and the note's **own author** all get `NOTE_GONE` with byte-identical bodies, because `app.visible_notes` gates on `recipient_id = viewer_id` and this read composes it with an id predicate rather than a second `where` (ADR-0002 §6, §10, B17). A malformed id is refused at the input schema as a wire concern, never as an existence check. §6a is re-evaluated on this read like any other: a severed connection drops the whole author card and the author's `app.users.id` appears nowhere in the payload, while a lowered disclosure keeps the card and drops the name. Feature: `specs/features/pin-a-note.feature`; decision D14 |
| Pin-back is a new note, and only offered when there is somebody to address ([#176](https://github.com/drewdrewthis/playa-post/issues/176)) | `test:unit` | `apps/web/src/app/notes/note-pin-back.unit.test.ts`, `apps/web/src/app/notes/note-detail-sheet.unit.test.tsx`, `modules/notes/tests/unit/get-note.query.unit.test.ts` | **AC2** (a note can be pinned back from the expanded view): the control routes into the existing composer with the author preselected (`/board/new?noteTo=<authorId>`), reusing `notes.pin` and its first-degree gate — no new mutation, no lifecycle, no migration. Four states, kept apart on purpose: an author card present at degree 1 offers the control; a card present further away states the distance and offers nothing (a present card is not a reachable person — pinning required degree 1 *at the time* and the read never re-derives it); **an absent author card offers nothing at all**, asserted at the mount with the viewer's own graph seeded so the "no derived recipient" claim is load-bearing rather than a tautology (§6a, B5); a graph read that has not landed is its own silence (`unknown-distance`, named for what is missing rather than for waiting, since a read that settled with an error is just as unable to say how far away somebody is), so it never renders as "not connected". Server half: the `null`-to-`NOTE_GONE` translation is one class with no branch that could grow a second. Decision D14 |
| Pin-back, end to end ([#176](https://github.com/drewdrewthis/playa-post/issues/176)) | `test:e2e` | `tests/e2e/vertical-slice-e2e.spec.ts` | A pins a note to B; B taps the card, reads it in the expanded view, follows the pin-back control into the composer, and the answer lands on A's board **as a note of its own** — which is decision D14 asserted where it is observable: pinning back wrote a new note and changed nothing about the one it answered. Last describe in the file, deliberately: a note has no take-down (D6's corollary, kept by D14) and the e2e database is shared across alphabetically-ordered specs, so the two notes it pins must land where nothing later reads them. `test:e2e` is CI-advisory (`docs/engineering/l5-plan.md` D2), so the gating proof is the `test:integration` and `test:unit` rows above |
| ~~Notify Me on several saved views at once ([#172](https://github.com/drewdrewthis/playa-post/issues/172))~~ **Removed by [#208](https://github.com/drewdrewthis/playa-post/issues/208) / ADR-0019** | — | — | Saved Views and the per-view bells are gone; #172's ACs describe a feature that no longer exists. What survives of its proof surface is the one-match-per-person guard (next row) and the migration suite, both retargeted to #208's shape. Struck rather than deleted so the index stays a history of what was proved, not only of what is |
| Notify Me — one notification per person, however many rows the directory returns ([#172](https://github.com/drewdrewthis/playa-post/issues/172), kept by [#208](https://github.com/drewdrewthis/playa-post/issues/208)) | `test:unit` | `modules/notifications/tests/unit/evaluate-notify-me-multiple-queries.unit.test.ts` | Defence in depth since #208: `unique (owner_id)` holds a person to one stored query, but the evaluator still settles a person at their first match, so a duplicate directory row degrades to a duplicate read and never a duplicate push. The web half of #172 (bell refusals, the Saved screen walk) went with the feature |
| Uniform soft delete, and the purge that makes it finite ([#169](https://github.com/drewdrewthis/playa-post/issues/169)) | `test:integration` | `tests/integration/soft-delete-purge.integration.test.ts` | **AC1** (every user-facing delete is soft): bulletins are the sole store since [#208](https://github.com/drewdrewthis/playa-post/issues/208) / ADR-0019 removed saved views and their `deleted_at` with them — `archived_at` was already the soft delete (D9), so AC1 is M2-AC12, unchanged. **AC4** (verifiable in both directions) is the boundary block: 31 days purged and 29 days retained, one assertion carrying both sides, because a one-sided boundary test passes for an implementation that deletes everything. **AC2/AC3** (a purge exists; the window is configuration-driven): driven through the real `buildAppContainer`, which is the only place the `targets` array lives, so each store is proved reachable from configuration all the way to its `DELETE` — the count *and* the cutoff are asserted per target, which is where `purgeRetentionDays` arriving at that particular store becomes observable. ⚠ Scope, stated because the wording invites more: this catches a target **removed** from the array, not a future soft-deleted table that never gets one — no test can fail for a decision nobody made. The standing requirement that a new `deleted_at` ships with its target in the same commit is D17's, not this suite's. The window is varied to 7 days and rows at 8 and 6 days settle it, neither of which a hardcoded 30 could pass. Also here: a bulletin with a report and a dismissal purges **cleanly** — the pre-cascade failure was an exception, not a wrong count, and any reported bulletin was enough to wedge the sweep permanently and silently — while a retained bulletin's own report and dismissal stay put; the sweep writes zero outbox rows; an expired-but-never-removed bulletin is untouched; **AC5** (the notes decision) is asserted positively — a year-old note survives the sweep — so "we did not build that" is recorded rather than inferred from silence. **AC6** (saved views' hard delete replaced) was AC1's first half until #208 dropped the table. **AC7** ([#118](https://github.com/drewdrewthis/playa-post/issues/118) as prior art) is D17's scope note: ADR-0006's outbox and `mutation_results` prunes stay #118's, deliberately. Feature: `specs/features/soft-delete-purge.feature`; decision D17, amending D9 |
| Saved Views removed; Notify Me stands alone ([#208](https://github.com/drewdrewthis/playa-post/issues/208)) | `test:integration` | `modules/views/tests/integration/notify-me-queries-schema-migration.integration.test.ts` | Catalog half: `id` stays the primary key (outbox aggregate routing), `owner_id` is unique on its own, `source_view_id` is gone, and a second query per owner is refused naming `notify_me_queries_owner_id_key`. Scenario half applies `supabase/migrations` in two halves around `20260813214946_remove_saved_views.sql`: an untied query seeded pre-migration survives with its text and version, and a designated one is deleted with its view. Also: `tests/security/app-table-inventory.security.test.ts` (the table leaves the inventory by name), `tests/fitness/contracts-api-parity.fitness.test.ts` (five procedures fewer), and B13's surviving `notifyMe.update` block in `tests/security/write-path-idor-bulletins.security.test.ts`. Feature: `specs/features/notify-me.feature`; decision D19; ADR-0019 |
| Soft delete — the sweep's arithmetic, its schedule, and the retention key ([#169](https://github.com/drewdrewthis/playa-post/issues/169)) | `test:unit` | `apps/server/src/entrypoints/purge/{purge-soft-deleted-rows,start-purge-poller}.unit.test.ts`, `packages/configuration/src/load-configuration.unit.test.ts`, `tests/fitness/{main-starts-every-poller,one-bulletin-delete-site}.fitness.test.ts` | The cutoff is `now` less the **target's own** window (asserted at two different windows, so a constant fails; and at two different windows *within one round*, which is the shape a single round-wide number could not express — ADR-0006's other retention chores have windows already decided). **One clock reading per round, subtracted per target**: two targets configured alike get an identical instant, where a clock read inside the loop would let them disagree about where the window starts, and a row deleted in the gap would be swept from one store and kept in the other with nothing to surface the discrepancy. A refusing target ends the round rather than being stepped over: the sweep is idempotent and hourly, so stopping early costs one interval, while catching per target would let the log report success for a store unswept for months. The poller is held to its siblings' contract (no round before the interval, no overlapping rounds, `onError` and keep going, `stop()` waits for the in-flight `DELETE` before the pool is disposed) plus the one respect in which it differs — it reports a round that removed rows and stays silent on one that did not, because the steady state is zero and an hourly line reading zero trains a reader to skip the one that matters. That reporting callback runs **outside** the round it describes: a throwing observer is neither reported as a failed sweep (which would send somebody after a database problem that does not exist) nor allowed to reject the in-flight promise `stop()` awaits (which would turn a clean SIGTERM into a failed shutdown), and both halves are asserted. Configuration: `PURGE_RETENTION_DAYS` defaults to 30, coerces from the string a process receives, and refuses zero, negative, fractional, and unparseable values naming the key and never its value — a zero window is a hard delete wearing a sweep's name, and silent, since every read already hides the row. Two structural facts are guarded here rather than left to a reviewer's grep, both being preconditions that fail silently: **`main.ts` starts and stops every poller** — the poller set is read off `entrypoints/` rather than listed, so a fourth loop added and never started fails too, and a sweep that exists but never runs is AC2 unmet with nothing to show for it; and **the retention sweep is the only statement in the server that deletes an `app.bulletins` row**, which is the entire argument for the schema's first `ON DELETE CASCADE`. A second delete site would inherit that cascade without anybody deciding it should, destroying a viewer's private report (B9) as a side effect of somebody else's action, invisibly from the call site |
| Read-path indistinguishability extends to the note surface ([#176](https://github.com/drewdrewthis/playa-post/issues/176)) | `test:security` | `tests/security/bulletin-indistinguishability.security.test.ts` | B17 gains `notes.getById`, its assertion text absorbing the new surface the way B13's absorbed `bulletin.undismiss` — `provenBy` is a single path, so a B-row's proof lives in one file and the row rather than the filename is the unit of coverage. Three inputs, not the bulletin case's two: the note surface has a third excluded reader, its **own author**, refused byte-identically to a stranger. A positive control asserts the recipient really can read it, without which a query that refused everybody would pass every other assertion. The serialized refusal is checked to carry neither the note's body nor the author's identifier |
